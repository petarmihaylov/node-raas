{
  "name": "@petarmihaylov/node-raas",
  "description": "A tiny library and CLI for interacting with the Reports as a Service (RAAS) API from UKG (Ultimate Kronos Group). \nThis project is maintained by the team behind RaasTastic and the community. It provides a balanced set of features that should suit a broad range of use-cases of the Reports as a Service API from UKG.\n\nFor a production-ready, feature-rich solution that supports advanced use cases like support for long-running reports and executing reports with parameters, consider using RaasTastic.\n\nRun 'node-raas raastastic' or https://raastastic.com/ to learn more.\n\nNOTE: node-raas is not endorsed and not maintained by UKG.\nSupport requests for node-raas should be directed to the developer and contributors and not UKG or the UKG support team.",
  "version": "0.0.1",
  "author": "Petar Mihaylov @petarmihaylov",
  "bin": {
    "node-raas": "./bin/run"
  },
  "bugs": "https://github.com/petarmihaylov/node-raas/issues",
  "dependencies": {
    "@oclif/command": "^1",
    "@oclif/config": "^1",
    "@oclif/plugin-help": "^3",
    "chalk": "^4.1.2",
    "cli-ux": "^5.6.3",
    "inquirer": "^8.2.0",
    "soap": "^0.43.0",
    "tslib": "^1",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@oclif/dev-cli": "^1",
    "@oclif/test": "^1",
    "@types/chai": "^4",
    "@types/inquirer": "^8.1.3",
    "@types/mocha": "^5",
    "@types/node": "^16.11.9",
    "@types/sax": "^1.2.3",
    "@types/uuid": "^8.3.1",
    "chai": "^4",
    "eslint": "^7.32.0",
    "eslint-config-oclif": "^3.1",
    "eslint-config-oclif-typescript": "^0.1",
    "globby": "^10",
    "mocha": "^5",
    "nock": "^13.2.1",
    "nyc": "^14",
    "rxjs": "~6.6.0",
    "ts-node": "^8",
    "typescript": "^3.3"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "files": [
    "/bin",
    "/lib",
    "/npm-shrinkwrap.json",
    "/oclif.manifest.json"
  ],
  "homepage": "https://github.com/petarmihaylov/node-raas",
  "keywords": [
    "oclif"
  ],
  "license": "MIT",
  "main": "lib/index.js",
  "oclif": {
    "commands": "./lib/commands",
    "macos": {
      "identifier": "com.node-raas.command"
    },
    "bin": "node-raas",
    "plugins": [
      "@oclif/plugin-help"
    ]
  },
  "repository": "petarmihaylov/node-raas",
  "scripts": {
    "postpack": "rm -f oclif.manifest.json",
    "posttest": "eslint . --ext .ts --config .eslintrc",
    "prepack": "rm -rf lib && tsc -b && oclif-dev manifest && oclif-dev readme",
    "test": "nyc --extension .ts mocha --forbid-only \"test/**/*.test.ts\"",
    "test:watch": "nyc --extension .ts mocha --forbid-only \"test/**/*.test.ts\" tsc --watch",
    "version": "oclif-dev readme && git add README.md"
  },
  "types": "lib/index.d.ts"
}
